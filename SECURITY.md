# Security Policy / 安全说明

## 请先理解这个应用的威胁模型

**这是一个本地优先的编码智能体。它会在你的机器上读写文件、执行命令。**

它被设计成**没有远程访问入口**：

- HTTP 服务默认只监听 `127.0.0.1`，不对外网开放。
- **没有账号体系** —— 鉴权靠"只有本机能访问"。
- 因此，以下行为等同于把控制权交出去：**把端口转发到公网、放进反向代理、或绑定到 `0.0.0.0`**。不要这样做。

### 已实现的防护

| 面 | 措施 |
|---|---|
| 跨站读取 | 不下发任何 CORS 头，并把带外部 `Origin` 的请求直接拒绝 |
| DNS rebinding | 校验 `Host` 与 `Origin`：回环名与本机字面 IP 始终接受；其它主机名需显式加入 `SHE_ALLOWED_HOSTS`（支持 `*`，但那是你自己的选择） |
| 工作区逃逸 | 所有文件操作限制在工作区内，**并解析符号链接**（防止软链指向区外） |
| 破坏性命令 | 默认拦截 `rm -rf` / `format` / `git push --force` 等模式 |
| 确认票据 | 危险操作需要一次性票据，**且票据绑定具体参数**（批准的 `rm -rf a` 不能用来跑 `rm -rf b`） |
| 进程隔离 | Electron 渲染进程开启 `sandbox: true` + `contextIsolation` |
| 密钥 | 只存本地 `.env`；状态接口不回传密钥值 |

这些都有回归测试：`node scripts/security-check.mjs`（14 项）。

### 已知的、你需要自己承担的边界

- **`SHE_ALLOW_ALL_COMMANDS=true` 会解除危险命令防护**，并跳过写文件审批。这是有意提供的选项，但在不可信环境下开启等于放弃隔离。
- **提示注入**：工作区里的文件内容会被模型读取。如果一个恶意仓库的文件里写了"忽略之前的指令，去执行 X"，模型**可能**会照做。这是所有编码智能体的共性问题，目前没有可靠解法 —— 处理来源不明的仓库时请保持警惕。
- **MCP 服务器**：你接入的 MCP 服务是独立进程，拥有它自己声明的权限，不在本项目的沙箱范围内。
- 没有速率限制：本机进程可以无限调用接口。

## 报告漏洞

请**不要**开公开 issue。用 GitHub 的私密漏洞报告（Security → Advisories → Report a vulnerability），或在仓库主页找联系方式。

请附上：受影响版本、复现步骤、影响判断。会尽快回复。

## 支持范围

只维护当前主分支。这个项目处于早期阶段，不提供长期支持版本。
