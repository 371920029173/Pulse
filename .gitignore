node_modules/
dist/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
.env
.she/
*.tsbuildinfo

# One-off debug probes (not part of the product)
_*.mjs

# Local run logs
_srv.*.log
*.log

# 打包产物（node scripts/release.mjs 生成）
release/
_*.js
_*.py
*.corrupt-*

# Electron staged runtime (rebuilt by stage-desktop-runtime.mjs)
packages/desktop/runtime/

# Playwright MCP server 的页面快照，落在它运行时的 cwd 下。
# MCP 接通之后才会出现，而且每页一份 —— 不进仓库。
.playwright-mcp/

